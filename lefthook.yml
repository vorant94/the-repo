pre-push:
  commands:
    test:
      run: pnpm -r run --if-present test
    unused-code:check:
      run: pnpm run unused-code:check
pre-commit:
  commands:
    lint:ci:
      run: pnpm run lint:ci
    ts:check:
      run: pnpm -r run --if-present ts:check
