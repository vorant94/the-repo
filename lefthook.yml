pre-push:
  commands:
    test:all:
      run: npm run test:all
    knip:
      run: npm run knip
pre-commit:
  commands:
    lint:ci:
      run: npm run lint:ci
    ts:check:
      run: npx turbo ts:check
