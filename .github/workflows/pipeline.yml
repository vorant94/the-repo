name: App Pipeline

on:
  workflow_call:
    inputs:
      workspace:
        type: string
        required: true
    secrets:
      CF_API_TOKEN:
        required: false
      CF_ACCOUNT_ID:
        required: false

permissions:
  contents: read

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    outputs:
      has_e2e: ${{ steps.check-e2e.outputs.has_e2e }}
      has_deploy: ${{ steps.check-deploy.outputs.has_deploy }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 23.6
          cache: npm

      - name: Install Dependencies
        run: npm ci --no-audit

      - name: Check E2E
        id: check-e2e
        run: |
          [ -f "apps/${{ inputs.workspace }}/playwright.config.ts" ] \
            && echo "has_e2e=true" >> $GITHUB_OUTPUT \
            || echo "has_e2e=false" >> $GITHUB_OUTPUT

      - name: Check Deploy
        id: check-deploy
        run: |
          jq -e '.scripts["deploy:production"]' apps/${{ inputs.workspace }}/package.json > /dev/null 2>&1 \
            && echo "has_deploy=true" >> $GITHUB_OUTPUT \
            || echo "has_deploy=false" >> $GITHUB_OUTPUT

      - name: TS Check
        run: npx turbo ts:check --filter=${{ inputs.workspace }}

      - name: Test
        run: npx turbo test --filter=${{ inputs.workspace }}

      - name: Are Translations Sorted
        run: npx turbo are-translations-sorted --filter=${{ inputs.workspace }}

      - name: Build
        run: npx turbo build --filter=${{ inputs.workspace }}

      - name: Upload Build
        if: steps.check-deploy.outputs.has_deploy == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ inputs.workspace }}
          path: apps/${{ inputs.workspace }}/dist
          include-hidden-files: true
          retention-days: 1

  e2e:
    name: E2E
    runs-on: ubuntu-latest
    needs: [ci]
    if: needs.ci.outputs.has_e2e == 'true'
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node
        id: setup-node
        uses: actions/setup-node@v4
        with:
          node-version: 23.6
          cache: npm

      - name: Install Dependencies
        run: npm ci --no-audit

      - name: Define Playwright Version
        id: playwright-version
        run: |
          PLAYWRIGHT_VERSION=$(cat package-lock.json | jq -r ".packages[\"node_modules/@playwright\/test\"].version")
          echo "version=$PLAYWRIGHT_VERSION" >> $GITHUB_OUTPUT

      - name: Cache Playwright Browsers
        id: cache-playwright
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-${{ steps.setup-node.outputs.node-version }}-playwright-${{ steps.playwright-version.outputs.version }}-${{ hashFiles(format('apps/{0}/playwright.config.ts', inputs.workspace)) }}

      - name: Install Playwright with Dependencies
        if: steps.cache-playwright.outputs.cache-hit != 'true'
        run: npx turbo e2e:install --filter=${{ inputs.workspace }}

      - name: Install Playwright Dependencies
        if: steps.cache-playwright.outputs.cache-hit == 'true'
        run: npx turbo e2e:install-deps --filter=${{ inputs.workspace }}

      - name: Run E2E
        run: npx turbo e2e --filter=${{ inputs.workspace }}

      - name: Upload E2E Report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-report-${{ inputs.workspace }}
          path: apps/${{ inputs.workspace }}/playwright-report/
          retention-days: 30

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: [ci, e2e]
    if: |
      always() &&
      needs.ci.result == 'success' &&
      (needs.e2e.result == 'success' || needs.e2e.result == 'skipped') &&
      needs.ci.outputs.has_deploy == 'true' &&
      (
        (github.event_name == 'push' && github.ref == 'refs/heads/master') ||
        (github.event_name == 'pull_request' &&
          contains(github.event.pull_request.labels.*.name, 'preview') &&
          contains(github.event.pull_request.labels.*.name, inputs.workspace))
      )
    concurrency:
      group: deploy-${{ inputs.workspace }}
      cancel-in-progress: false
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 23.6
          cache: npm

      - name: Install Dependencies
        run: npm ci --no-audit

      - name: Download Build
        uses: actions/download-artifact@v4
        with:
          name: build-${{ inputs.workspace }}
          path: apps/${{ inputs.workspace }}/dist

      - name: Migrate
        run: npx turbo migrate:production --filter=${{ inputs.workspace }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      - name: Deploy Production
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        run: npx turbo deploy:production --filter=${{ inputs.workspace }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      - name: Deploy Preview
        if: |
          github.event_name == 'pull_request' &&
          contains(github.event.pull_request.labels.*.name, 'preview') &&
          contains(github.event.pull_request.labels.*.name, inputs.workspace)
        run: npx turbo deploy:preview --filter=${{ inputs.workspace }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
